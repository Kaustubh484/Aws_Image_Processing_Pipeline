{
  "name": "file-processor",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && cd ../lambda/file-processor && npm run build && cd ../stream-processor && npm run build && cd ../../cdk && cdk deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.19.41",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  }
}
